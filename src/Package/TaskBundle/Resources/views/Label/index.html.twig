{% extends 'PackageTaskBundle::base.html.twig' %}

{% block title %}
    {{ 'Labels'|trans }}
{% endblock %}
{% block body %}
    {{ parent() }}
    <article>
        <a href="{{ path('PackageTaskBundle:Label:Add')}}">
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
        </a>
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'Name'|trans({}, 'messages'), 'l.name')|raw }}</th>
                <th>{{ 'Action'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for labels in pagination %}
                <tr {% if loop.index is odd %}class="color"{% endif %}>
                    <td>{{ labels.name }}</td>
                    <td>
                        <a class="btn btn-sm" href="{{ path('PackageTaskBundle:Label:Edit', {'id':labels.id}) }}">
                            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                        </a>
                        <a class="btn btn-sm" href="{{ path('PackageTaskBundle:Label:Remove', {'id':labels.id}) }}">
                            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
    </article>
{% endblock %}